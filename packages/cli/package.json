{
  "name": "@revite/cli",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "build:cli:revite": "tsc cli/revite.ts --outDir cli/dist --moduleResolution node"
  },
  "dependencies": {
    "lodash-es": "^4.17.21"
  }
}
