{
  "name": "@revite/cli",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build",
    "lint": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "lodash-es": "^4.17.21"
  }
}
