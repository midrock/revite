import { Task } from '../core/Task'

export class ProvidersBootTask extends Task {
  async run() {
    // await Promise.all([
    //   ...manifests.map(manifest => {
    //     return revite.providers.boot(manifest)
    //   }),
    // ])
  }
}
